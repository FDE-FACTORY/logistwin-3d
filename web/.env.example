# ── 프론트엔드 환경변수 (Vite) ─────────────────────────────────
# 백엔드 WebSocket 주소. 배포 시 Railway 백엔드 URL로 설정.
VITE_WS_URL=http://localhost:3001

# Kakao 지도 JavaScript 키 (설정 시 '배송 관제'가 실지도로 전환, 없으면 시뮬 맵).
# VITE_KAKAO_MAP_KEY=your_kakao_javascript_app_key
